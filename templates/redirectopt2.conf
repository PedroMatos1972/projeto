server {
    listen       [PORT];
    server_name  [SERVERNAMENONWWW];
    return 301 http://[SERVERNAMEWWW];

    location ~* \.(jpg|jpeg|gif|png|ico)$ {
        expires 5d;

        [CACHE]

        proxy_pass [PROXY];
    }

    location / {
      proxy_pass [PROXY2];
    }
}
