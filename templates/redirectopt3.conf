server {
    listen       [PORT];
    server_name  [SERVERIP];
    return 301 http://[SERVERDOMAIN];

    location ~* \.(jpg|jpeg|gif|png|ico)$ {
        expires 5d;

        [CACHE]

        proxy_pass [PROXY];
    }

    location / {
      proxy_pass [PROXY2];
    }
}
